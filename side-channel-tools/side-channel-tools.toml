[project]
name = "side-channel-tools"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["host-src"]
include = ["shared*"]

[tool.setuptools.package-dir]
"" = "host-src"
